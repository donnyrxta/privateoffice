'use client';
import {useEffect,useState} from 'react';
import {Header} from './landing';
export default function SignInPanel({kind}:{kind:'agent'|'office'}){const[href,setHref]=useState('/signin-with-chatgpt?return_to=/'+kind);useEffect(()=>{setHref('/signin-with-chatgpt?return_to='+encodeURIComponent('/'+kind+location.hash))},[kind]);return <><Header/><main className="app-main prose"><p className="eyebrow">{kind==='agent'?'AGENT ACCESS':'PRIVATE OFFICE'}</p><h1 className="sign-in-title">{kind==='agent'?'Every visit starts with clarity.':'Your introductions. In one place.'}</h1><p className="muted">{kind==='agent'?'Sign in with the email on your invitation to see and accept your assigned visit.':'Sign in to manage enquiries, assign visits and review their progress.'}</p>{kind==='agent'&&<div className="notice section-gap"><strong>Location sharing is part of assigned visits.</strong><p>Opening the authenticated workspace starts your Private Office security session. Starting a customer visit opens a dedicated high-accuracy tracking epoch. The installed app is designed to continue active-visit tracking in the background; browser behavior depends on the platform and its location permissions.</p></div>}<div className="actions section-gap"><a className="button" href={href} target="_top">Sign in with ChatGPT</a><a className="button outline" href={'/'+kind+'/demo'}>Explore a demonstration</a></div><a href="/privacy" className="text-link">Read the location-sharing notice</a></main></>}
