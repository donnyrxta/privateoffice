import {sqliteTable,text,integer,real,index,uniqueIndex} from 'drizzle-orm/sqlite-core';
export const office=sqliteTable('office',{id:integer('id').primaryKey(),ownerId:text('owner_id').notNull(),ownerEmail:text('owner_email').notNull(),createdAt:integer('created_at').notNull()});
export const enquiries=sqliteTable('enquiries',{id:text('id').primaryKey(),name:text('name').notNull(),contact:text('contact').notNull(),interest:text('interest').notNull(),createdAt:integer('created_at').notNull(),status:text('status').notNull().default('new')});
export const visits=sqliteTable('visits',{id:text('id').primaryKey(),ownerId:text('owner_id').notNull(),agentName:text('agent_name').notNull(),agentEmail:text('agent_email').notNull(),agentId:text('agent_id'),clientName:text('client_name').notNull(),property:text('property').notNull(),meeting:text('meeting').notNull(),lat:real('lat').notNull(),lng:real('lng').notNull(),scheduledAt:integer('scheduled_at').notNull(),expiresAt:integer('expires_at').notNull(),createdAt:integer('created_at').notNull(),inviteHash:text('invite_hash').notNull(),clientHash:text('client_hash').notNull(),status:text('status').notNull().default('scheduled'),consentAt:integer('consent_at'),consentVersion:text('consent_version'),shareStartedAt:integer('share_started_at'),shareEpoch:text('share_epoch'),shareUntil:integer('share_until'),activeDeviceId:text('active_device_id'),lastSequence:integer('last_sequence').default(-1),trackingHealth:text('tracking_health').default('idle'),lastReceivedAt:integer('last_received_at'),stoppedAt:integer('stopped_at'),revokedAt:integer('revoked_at'),etaMinutes:integer('eta_minutes'),clientConfirmedAt:integer('client_confirmed_at'),propertyConfirmed:text('property_confirmed'),clientComment:text('client_comment')},t=>[index('visits_owner').on(t.ownerId,t.createdAt),index('visits_agent').on(t.agentId,t.createdAt),index('visits_invite').on(t.inviteHash),index('visits_client').on(t.clientHash)]);
export const points=sqliteTable('points',{id:text('id').primaryKey(),visitId:text('visit_id').notNull().references(()=>visits.id,{onDelete:'cascade'}),deviceId:text('device_id'),shareEpoch:text('share_epoch'),sequenceNumber:integer('sequence_number'),lat:real('lat').notNull(),lng:real('lng').notNull(),accuracy:real('accuracy').notNull(),altitude:real('altitude'),altitudeAccuracy:real('altitude_accuracy'),heading:real('heading'),speed:real('speed'),simulated:integer('simulated',{mode:'boolean'}).default(false),recordedAt:integer('recorded_at').notNull(),queuedAt:integer('queued_at'),receivedAt:integer('received_at').notNull(),qualityClass:text('quality_class'),plausibilityState:text('plausibility_state').default('normal'),payloadHash:text('payload_hash')},t=>[index('points_visit_time').on(t.visitId,t.receivedAt),uniqueIndex('points_visit_epoch_sequence').on(t.visitId,t.shareEpoch,t.sequenceNumber)]);
export const devices=sqliteTable('devices',{id:text('id').primaryKey(),agentId:text('agent_id').notNull(),publicKeyJwk:text('public_key_jwk').notNull(),platform:text('platform').notNull(),appVersion:text('app_version'),createdAt:integer('created_at').notNull(),lastSeenAt:integer('last_seen_at').notNull(),revokedAt:integer('revoked_at')},t=>[index('devices_agent').on(t.agentId,t.lastSeenAt)]);
export const agentSessions=sqliteTable('agent_sessions',{id:text('id').primaryKey(),agentId:text('agent_id').notNull(),deviceId:text('device_id').notNull().references(()=>devices.id),startedAt:integer('started_at').notNull(),lastSeenAt:integer('last_seen_at').notNull(),endedAt:integer('ended_at')},t=>[index('agent_sessions_agent').on(t.agentId,t.lastSeenAt)]);
export const securityEvents=sqliteTable('security_events',{id:text('id').primaryKey(),agentId:text('agent_id').notNull(),deviceId:text('device_id').notNull().references(()=>devices.id),sessionId:text('session_id'),visitId:text('visit_id'),shareEpoch:text('share_epoch'),kind:text('kind').notNull(),deviceAt:integer('device_at').notNull(),receivedAt:integer('received_at').notNull(),detail:text('detail'),payloadHash:text('payload_hash').notNull()},t=>[index('security_events_agent_time').on(t.agentId,t.receivedAt),index('security_events_visit_time').on(t.visitId,t.receivedAt)]);
export const events=sqliteTable('events',{id:text('id').primaryKey(),visitId:text('visit_id').notNull().references(()=>visits.id,{onDelete:'cascade'}),kind:text('kind').notNull(),actor:text('actor').notNull(),at:integer('at').notNull(),detail:text('detail')},t=>[index('events_visit_time').on(t.visitId,t.at)]);
export const rateLimits=sqliteTable('rate_limits',{key:text('key').primaryKey(),count:integer('count').notNull(),expiresAt:integer('expires_at').notNull()});
