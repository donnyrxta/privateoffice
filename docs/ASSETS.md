# Asset provenance

Connected Canva material inspected for Private Office includes:

- Folder `FAFnNqCSiZA` — **TierraViva Diamente Villa_jpg**
- Asset `MAFnNhJ_TxE` — `DG.AL_Diamente Villa Ext 1.jpg`, original metadata 6000 × 3636
- Additional Tierra Viva Esmeralda/Zafiro, DG1 and DaVinci folders are present in the connected Canva library.

The source package also contains a matching local Tierra Viva architectural render. The GitHub connector used for this implementation pass cannot reliably transfer that large binary without truncation, so the deployable checkpoint uses an official DarGlobal Tierra Viva image from `cdn.darglobal.co.uk` as the runtime hero instead of committing a corrupted JPEG.

The large `docs/reference/house-animation-reference.mp4` is a design reference only and is not required by the application runtime. It is omitted from this GitHub source import.

The render is treated as architectural marketing material, not evidence of current property availability or a current sales mandate.
