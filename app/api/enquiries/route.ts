import {body,db,json,wrap,str,rate,hash,HttpError} from '@/lib/server';
export async function POST(req:Request){return wrap(async()=>{const b=await body(req);if(b.website)return json({ok:true});if(b.consent!==true)throw new HttpError(400,'Please agree to be contacted.');await rate('enquiry:'+await hash(req.headers.get('cf-connecting-ip')||'unknown'),5,3600000);await db().prepare('INSERT INTO enquiries (id,name,contact,interest,created_at,status) VALUES (?,?,?,?,?,?)').bind(crypto.randomUUID(),str(b.name,100),str(b.contact,160,5),str(b.interest,2000),Date.now(),'new').run();return json({ok:true},201)})}
